#include<stdio.h>
#define LINHA 15
#define COLUNA 5
//! Faça um programa em C que lê uma matriz A[15,5] e a escreva. Verifique, a seguir, quais os elementos
//! de A que estão repetidos e quantas vezes cada um está repetido. Escrever cada elemento repetido com
//! uma mensagem dizendo que o elemento aparece X vezes em A.
int main (){
    int a[LINHA][COLUNA],h,i,j,l=0,c=0,contador;

    //! gravar o valor 
    for(i = 0 ; i < LINHA ; i ++){
        for(j = 0 ; i < COLUNA ; j ++){
            printf("digite o valor: ");
            scanf("%d",&a[LINHA][COLUNA]);
        }
    }

    for(h = 0 ;  < (LINHA * COLUNA) - 1; h++1){
        contador= 0;
        if(){
            
        }
        a[l][c];
        for(i = 0; i < LINHA ; i ++){
            for(j=0 ; j < COLUNA ; j++ ){
              a[i][j];
              

            }
        }
    }













    return 0 ;
}